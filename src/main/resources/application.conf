ktor {
    deployment {
        port = 8101
        port = ${?PORT}
        rootPath = "varseldb-api"
    }

    application {
        modules = [no.nav.personbruker.varseldb.api.config.BootstrapKt.mainModule]
    }
}
